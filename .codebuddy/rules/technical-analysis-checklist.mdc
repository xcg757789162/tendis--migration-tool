---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-02T07:34:58.870Z
provider: 
---

# 技术方案分析检查清单

在分析任何技术方案的可行性之前，必须完成以下检查：

## 1. 场景假设验证

在得出"不可行"结论之前，必须明确回答：

- **数据规模假设**：
  - 总数据量是多少？
  - **实际需要处理的数据量是多少？**（可能只是子集！）
  - 是全量迁移还是部分迁移？

- **处理方式假设**：
  - 是批量处理还是流式处理？
  - 需要维护状态的数据量是多少？
  - 服务端能做多少预处理？

## 2. 原生能力检查

在设计自定义方案之前，必须检查：

- Redis/Tendis 是否有原生命令可以替代？
  - `SCAN MATCH` 服务端过滤
  - `PSYNC` 复制协议
  - `binlog read` 增量日志
  
- 这些原生机制是否比自定义方案更高效？

## 3. 关键质疑

对于每个"不可行"的结论，必须回答：

1. 这个结论基于什么假设？假设是否可以被打破？
2. 是否有我没考虑到的优化方案？
3. 是否有原生机制可以替代自定义实现？
4. 用户真正需要的是什么？（可能和我假设的不同）

## 4. 40 亿 Key 迁移专项检查

- [ ] 是否混淆了"全量迁移"和"按前缀迁移"？
- [ ] 内存估算是否考虑了服务端过滤？
- [ ] 是否利用了 `SCAN MATCH prefix*` 的服务端过滤能力？
- [ ] 增量同步是否可以用 PSYNC/Binlog 替代轮询？
- [ ] 流式处理是否可以避免存储全量 Key？