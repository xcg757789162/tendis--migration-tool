# tendis-migrate 核心需求检查清单

## ⚠️ 重要：每次修改代码或输出方案前，必须对照此清单检查！

---

## 📋 四大核心需求

### 需求 1：按前缀迁移或跳过
**描述**：支持按 Key 前缀过滤，可以只迁移指定前缀，或跳过指定前缀

**验证方法**：
- [ ] 是否支持 `key_filter.prefixes` 配置？
- [ ] 是否支持 `key_filter.exclude_prefixes` 配置？
- [ ] 是否支持 `key_filter.mode` (all/prefix/pattern)？

**当前状态**：✅ 已实现（KeyFilter 结构）

---

### 需求 2：40 亿 Key 高效迁移
**描述**：全量和增量迁移都不能 OOM，必须支持超大规模 Key 迁移

**验证方法**：
- [ ] 全量同步是否存储所有 Key 到内存？（❌ 不能）
- [ ] 增量同步是否存储所有 Key 到内存？（❌ 不能）
- [ ] 内存占用是否 < 2 GB？（✅ 必须）

**40 亿 Key 内存估算**：
| 数据结构 | 40 亿 Key 内存 | 是否可接受 |
|----------|---------------|------------|
| `map[string]bool` | ~160 GB | ❌ |
| `sync.Map` | ~120-150 GB | ❌ |
| `[]string` | ~80 GB | ❌ |
| Bloom Filter | ~5.7 GB | ⚠️ 勉强 |
| 不存储 | 0 | ✅ |

**当前状态**：
- 全量同步：✅ 已修复（移除 processedKeys）
- 增量同步 V2：✅ 已实现（时间窗口模式）

---

### 需求 3：崩溃恢复不丢数据
**描述**：任何崩溃场景（网络、工具、集群）下，数据不丢失，任务可恢复

**验证方法**：
- [ ] 全量同步是否有断点（SCAN cursor）？
- [ ] 增量同步是否有断点？
- [ ] 任务状态是否持久化？
- [ ] 是否有优雅关闭机制？
- [ ] 重启后能否自动加载恢复？

**当前状态**：✅ 已实现
- 全量断点：每 10000 Key 或 30 秒保存
- 增量断点 V2：每 30 秒保存
- 任务状态：每 30 秒保存
- 优雅关闭：SIGINT/SIGTERM 处理

---

### 需求 4：冲突 Key 记录审查
**描述**：遇到目标端已存在的 Key，可以覆盖或跳过，且记录下来供人工审查

**验证方法**：
- [ ] 是否支持 `conflict_policy`（skip/replace）？
- [ ] 冲突 Key 是否记录？
- [ ] 记录上限是否足够（建议 100 万）？
- [ ] 超限后是否落盘？

**当前状态**：✅ 已实现
- conflict_policy：支持 skip/replace/skip_full_only
- ErrorKeys 上限：100 万（10 万内存 + 落盘）
- 落盘机制：超过 10 万自动落盘

---

## 🔍 方案输出前的自检流程

每次修改代码或输出方案前，必须回答以下问题：

```
□ 需求 1：前缀过滤功能是否正常工作？
□ 需求 2：40 亿 Key 场景会不会 OOM？
  - 全量同步内存占用？
  - 增量同步内存占用？
  - 是否有大规模数据结构存储 Key？
□ 需求 3：各种崩溃场景能否恢复？
  - 断点是否保存？
  - 重启后能否继续？
□ 需求 4：冲突 Key 是否记录？上限是否足够？
```

---

## ⚠️ 常见陷阱

### 陷阱 1：使用 map/sync.Map 存储 Key
```go
// ❌ 错误：40 亿 Key 会 OOM
processedKeys := sync.Map{}
knownKeys := make(map[string]bool)

// ✅ 正确：不存储 Key，或使用流式处理
```

### 陷阱 2：增量同步存储全量 Key 做对比
```go
// ❌ 错误：存储全量 Key 对比新增
knownKeys := scanAllKeys()  // 40 亿 Key = OOM
for key := range currentKeys {
    if !knownKeys[key] { migrate(key) }
}

// ✅ 正确：时间窗口模式
scanWithCallback(func(key string) {
    if OBJECT_IDLETIME(key) < 30s {
        migrate(key)  // 最近修改过
    }
})
```

### 陷阱 3：断点保存间隔过长
```go
// ⚠️ 风险：保存间隔过长会丢失更多进度
checkpointInterval := 10 * time.Minute  // 太长

// ✅ 推荐：每 30 秒或每 10000 Key
checkpointInterval := 30 * time.Second
checkpointKeyCount := 10000
```

---

## 📊 当前实现状态总结

| 核心需求 | 状态 | 实现方式 |
|----------|------|----------|
| **1. 前缀过滤** | ✅ | KeyFilter 结构 |
| **2. 40 亿 Key 无 OOM** | ✅ | 全量：无 processedKeys；增量 V2：时间窗口 |
| **3. 崩溃恢复** | ✅ | 断点持久化 + 优雅关闭 |
| **4. 冲突记录** | ✅ | ErrorKeys 100 万上限 + 落盘 |

**所有核心需求均已满足！**

---

## 📝 版本记录

| 日期 | 修改 | 原因 |
|------|------|------|
| 2026-02-02 | 移除 processedKeys | 发现 40 亿 Key 会 OOM |
| 2026-02-02 | 添加优雅关闭 | 提升崩溃恢复能力 |
| 2026-02-02 | 创建此检查清单 | 避免遗漏核心需求 |
