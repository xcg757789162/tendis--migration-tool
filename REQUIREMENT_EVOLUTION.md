# Tendis-Migrate 需求演化过程

> 本文档从需求驱动视角记录项目的完整演化历程，包括用户需求的提出、方案讨论与争论、关键决策转折点。
> 整合自：CONVERSATION_DRIVEN_EVOLUTION.md、FULL_CONVERSATION_EVOLUTION.md

---

## 一、项目起源：需求背景与初始讨论

### 1.1 用户最初的需求（2026-01-20 前后）

用户提出了 Tendis/Redis 数据迁移的四大核心需求：

> "我们公司有一个 Tendis 集群，数据量非常大，大约 40 亿个 Key。现在需要把部分数据迁移到另一个集群。我需要一个工具能够：
> 1. 按 Key 前缀筛选要迁移的数据，也能排除某些前缀
> 2. 支持这么大规模的数据迁移，不能 OOM
> 3. 如果迁移过程中崩溃了，重启后能继续，不丢数据
> 4. 目标端如果已经有相同的 Key，我想跳过但要记录下来，之后可以审查"

### 1.2 技术挑战分析

```
40 亿 Key 的技术挑战：
┌─────────────────────────────────────────────────────────────────┐
│  挑战 1：内存限制                                                │
│  - 如果存储全量 Key 到内存：40亿 × 20字节 = 80GB                  │
│  - 加上数据结构开销：可能达到 120-150GB → OOM                     │
│                                                                 │
│  挑战 2：迁移时间                                                │
│  - 假设每秒迁移 10000 个 Key                                     │
│  - 40亿 / 10000 = 400000 秒 ≈ 4.6 天                            │
│  - 需要可靠的断点续传机制                                        │
│                                                                 │
│  挑战 3：增量同步                                                │
│  - 全量迁移期间，源端数据还在变化                                 │
│  - 如何检测和同步这些变化？                                       │
│                                                                 │
│  挑战 4：前缀过滤                                                │
│  - 40 亿 Key，即使只迁移特定前缀也需要扫描全量                    │
│  - 如何高效过滤？                                                │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 技术选型讨论

**用户问**："用什么语言开发比较好？"

**AI 建议使用 Go 语言**，理由：
1. 高性能并发（goroutine 天然适合大量 Key 并行迁移）
2. 单二进制部署，无依赖
3. 成熟的 Redis 客户端库（go-redis）
4. 内存控制更可控
5. 跨平台编译

**用户追问**："需要 Web 界面吗？"

**AI 建议**：做一个简单的 Web UI（Vue 3 + Element Plus），方便可视化查看迁移进度、配置参数、查看错误 Key 列表。

---

## 二、第一次方案分歧：40 亿 Key 迁移可行性争论

### 2.1 AI 最初的错误判断

在讨论增量同步方案时，AI 犯了一个严重的分析错误：

**AI 错误观点**：
> "对于 40 亿 Key 的增量同步，PSYNC/Binlog 方案不可行，因为：
> 1. 需要实现完整的 Redis 主从复制协议，工作量巨大
> 2. PSYNC 返回的是全量数据流，无法按 Key 前缀过滤
> 3. 内存占用会很大
>
> 建议使用轮询 + 对比的方式：先 SCAN 全量 Key 存储，然后定期重新 SCAN 对比找出新增/修改的 Key。"

### 2.2 用户的关键纠正（转折点）

用户敏锐地指出了错误：

**用户反驳**：
> "你的分析有问题。你混淆了两个场景：
> 1. '全量迁移 40 亿 Key' —— 确实需要处理全量数据
> 2. '按前缀迁移子集' —— 实际迁移的可能只是几百万个 Key
>
> 而且你忽略了几个关键点：
> - `SCAN MATCH prefix*` 是**服务端过滤**，不是客户端过滤
> - PSYNC/Binlog 是**流式处理**，不需要存储全量 Key
> - 前缀过滤是 O(1) 的字符串比较，性能开销很小"

### 2.3 AI 的反思与修正

> **正确的理解**：
> 1. 全量同步：使用 SCAN + MATCH 流式处理，边扫描边迁移，不存储 Key
> 2. 增量同步：可以用 OBJECT IDLETIME 检测最近修改的 Key，也是流式处理
> 3. 如果 Tendis 支持 Binlog，还可以用更高效的 Binlog 方式
>
> **内存估算修正**：
> - 原估算：存储 40 亿 Key = 80-150GB（OOM）
> - 正确估算：流式处理，只保存当前批次 < 100MB

### 2.4 确立核心设计原则

经过这次讨论，确立了四大核心原则：

1. **绝对不能用 map/sync.Map 存储全量 Key**（40亿 Key = OOM）
2. **全量同步使用流式 SCAN**（边扫描边处理，不存储）
3. **增量同步使用时间窗口/Binlog**（不存储 Key 列表）
4. **ErrorKeys 必须有上限保护**（10 万内存 + 落盘）

---

## 三、V1 版本：基础功能需求

### 3.1 Key 过滤器设计

**用户问**："Key 过滤怎么实现？"

设计了三种过滤模式：
- **prefix 模式**（默认）：`prefixes: ["user:", "order:"]` + `excludes: ["user:temp:"]`
- **pattern 模式**：正则匹配，适合复杂场景
- **keylist 模式**：指定 Key 列表，适合精确迁移

**用户追问**："SCAN MATCH 能用上吗？"

→ 单前缀时使用 `SCAN MATCH prefix*` 服务端过滤，多前缀时 SCAN 全量 + 客户端过滤。

### 3.2 冲突策略设计

**用户问**："目标端已存在的 Key 怎么处理？"

设计了四种策略：
- `skip`：跳过不覆盖
- `replace`：直接覆盖
- `error`：报错停止
- `skip_full_only`：全量跳过 + 增量覆盖（推荐）

所有被跳过的 Key 记录到 ErrorKeys 列表，支持分页查询、导出。

### 3.3 V1.0 完成（2026-01-23）

实现功能：单任务全量迁移、Key 过滤器、冲突策略、动态 Worker Pool、Web UI、REST API、智能配置推荐。

### 3.4 V1.1 动态调整需求（2026-01-25）

**用户新需求**："迁移过程中能否动态调整 Worker 数量和 QPS？"

→ 实现了动态调整 Worker 数量、QPS 限制、批次大小。

---

## 四、测试环境搭建与发现

### 4.1 Docker + Tendis 集群搭建

用户提供 Mac 服务器，使用 Docker 部署 Tendis 2.7.0 集群。

### 4.2 关键发现：Tendis 限制

- **Tendis 2.7.0 不支持 `cluster-announce-ip`**：需要使用容器内部 IP
- **编译版本问题**：Mac 服务器需要 darwin 版本，公司需要 linux 版本

---

## 五、V2 架构升级需求

### 5.1 性能瓶颈

**用户**："V1 版本测试下来感觉性能不够，能提升吗？"

V1 瓶颈分析：单进程限制、断点粒度粗、无法分布式扩展。

### 5.2 Master-Worker 架构

提出 Master-Worker 多进程架构，用户补充关键需求：
> "Worker 需要能够独立运行，支持分布式部署"

### 5.3 Slot 分片迁移

**用户**："能否按 Slot 粒度进行迁移和断点？"

16384 Slot 分片 → 断点粒度从任务级提升到 Slot 级（重启后最多重做 0.006%）。

### 5.4 V2.0 Phase 1-2 实现（2026-01-28 ~ 2026-01-29）

Master-Worker 进程架构 + IPC 通信 + SQLite 元数据 + 16384 Slot 分片迁移。

---

## 六、增量同步方案讨论

### 6.1 三种方案对比

**用户问**："增量同步用什么方案？轮询还是 PSYNC？"

| 方案 | 优点 | 缺点 | 工作量 |
|------|------|------|--------|
| PSYNC 协议 | 实时性高 | 实现复杂 | 2-3 月 |
| Tendis Binlog | 实时性高，流式 | 需要 Tendis 支持 | 1-2 周 |
| 时间窗口（OBJECT IDLETIME） | 通用性好 | 实时性低（分钟级） | 3-5 天 |

**决策**：先实现时间窗口模式，再验证 Tendis Binlog 支持。

### 6.2 FakeSlave 模式探索

研究 Tendis 2.7.0 源码，发现可实现 FakeSlave 模式（INCRSYNC 协议），实时性毫秒级。

**用户**："先把设计文档写出来，等有时间再实现。"

→ 创建 `FAKE_SLAVE_DESIGN.md`

---

## 七、公司环境部署与问题发现

### 7.1 环境切换

用户到公司，提供新测试环境（10.248.37.11），需编译 Linux 版本、建立标准部署流程。

### 7.2 P0-P3 问题（2026-02-02）

大规模测试中发现严重问题：

| 问题 | 严重程度 | 根因 | 修复 |
|------|----------|------|------|
| P0: processedKeys OOM | 致命 | sync.Map 存储全量 Key | 移除，直接流式处理 |
| P1: 增量 V1 存全量 Key | 致命 | 存储全量 Key 对比差异 | 改为时间窗口模式 |
| P2: ErrorKeys 无上限 | 高 | 无限增长可能 OOM | 10 万内存 + 落盘 |
| P3: 断点保存不完整 | 高 | 崩溃丢失进度 | 定期保存 + 优雅关闭 |

### 7.3 前后端 API 对齐（2026-02-04）

**用户**："前端有些功能点击没反应"

排查发现 API 覆盖率只有 79%，补齐到 100%。

---

## 八、从 Redis-Shake 学习优化（2026-02-03）

**用户**："Redis-Shake 是怎么做的？能不能借鉴一下？"

借鉴的优化：
- 并发 Pipeline Writer（写入 QPS 10k → 40k+）
- 大 Key 分批处理（HSCAN/SSCAN/ZSCAN）
- 异步命令执行

---

## 九、Binlog 深度分析与 Bug 修复（2026-02-05）

### 9.1 用户要求深度分析 Tendis 源码

**用户**："我希望你再全面仔细分析一下 binlog_tool.cpp，然后检查我们现在的代码还有没有隐藏的 bug"

### 9.2 发现 3 个隐藏 Bug

1. **RESP 解析 Bug**：`strings.Split` 处理含 `\r\n` 的二进制数据会错误分割 → 改为按长度读取
2. **ReplOp 枚举不一致**：iota 生成的值与 Tendis 源码 `record.h` 不符 → 显式赋值
3. **cmdStr 格式判断缺失**：未区分 RESP 格式和非 RESP 格式 → 检查首字节 `*`

---

## 十、v2.3.0 BUG 修复版（2026-02-09）

### 10.1 key_filter.prefixes 参数不生效

**根因**：配置 `prefixes` 但 `mode` 字段为空，过滤器未启用。
**修复**：自动设置 `mode = "prefix"`。

### 10.2 增量同步 RocksDB Key 解析

**根因**：RocksDB RecordKey 含 9 字节头部 + varint 长度前缀，原解析不完整。
**修复**：正确处理 varint 长度前缀。

### 10.3 增量 Key 过滤功能验证

测试结果：匹配前缀的 Key 正常同步，不匹配的正确过滤（6 synced / 6 filtered）。

---

## 十一、v2.3.1 大规模测试（2026-02-10 ~ 2026-02-11）

50 亿 Key 测试环境 A 压力测试，发现 8 个 BUG：

| BUG | 问题 | 根因 |
|-----|------|------|
| 1 | WebSocket 指标字段缺失 | `sendTaskMetrics` 遗漏字段 |
| 2 | full_only 模式显示增量面板 | 前端未排除 full_only |
| 3 | 运行时参数修改不生效 | 只更新值未调用 `SetWorkerCount()` |
| 4 | 增量阶段 Key Filter 不生效 | `processBinlogEntries` 未检查 Filter |
| 5 | 纯增量模式不启动 FakeSlave | `needIncremental` 条件不全 |
| 6 | FakeSlave 连接后立即停止 | 增量阶段入口条件不包含 incremental |
| 7 | 集群拓扑缓存 `:0` | go-redis 缓存旧地址 |
| 8 | DBSIZE 超时回退 10000 | 50 亿 Key 超时 fallback 硬编码 |

---

## 十二、v2.4.0 新功能（2026-02-11 ~ 2026-02-12）

### 12.1 Preflight Check 迁移前校验

校验项：源端/目标端连通性、集群状态、Binlog 配置、版本兼容性、节点角色。

### 12.2 集群拓扑自动刷新

30 秒自动刷新 + 启动时强制验证 + 检测无效节点告警。

### 12.3 FakeSlave IP 自动探测

`getOutboundIP()` 自动探测本机到源端的出口 IP。

### 12.4 环境代码回滚（核心原则确立）

**用户**："环境是环境，工具是工具。绝对不要修改 tendis-migrate 工具的代码去适配某个特定的部署环境。"

→ 回滚 `buildAddrMapping` 代码，确认所有 `.go` 文件无环境污染。

### 12.5 版本发布

v2.4.0：Commit `0b7df2a`，37 files，+3822/-734。

---

## 附录：关键对话片段

### A. 40 亿 Key 方案争论

> **AI（错误）**："PSYNC 方案不可行，因为需要存储全量 Key..."
>
> **用户（纠正）**："你混淆了'全量迁移40亿Key'和'按前缀迁移子集'两个不同场景..."
>
> **AI（反思）**："您说得对，我的分析有问题。让我重新梳理..."

### B. 增量同步方案选择

> **用户**："增量同步用什么方案？"
>
> **AI**："考虑到 Tendis 2.7.0 的限制，建议使用时间窗口模式...如果验证通过 Binlog 支持，再切换到更高效的方案。"

### C. P0 问题修复

> **用户**："内存占用异常高"
>
> **AI**："发现 `processedKeys` sync.Map 存储了所有 Key，这是 OOM 的根本原因。解决方案是直接移除..."

### D. Binlog Bug 发现

> **AI**："发现 Bug！parseRESPCommand 使用 strings.Split 处理包含 \\r\\n 的二进制数据会出错"

### E. UI 优化讨论

> **用户**："任务 ID 悬浮提示框影响我点击任务名查看详情"

### F. 环境与工具分离

> **用户**："环境是环境，工具是工具。绝对不要修改代码去适配特定部署环境。"

---

## 统计汇总

| 指标 | 数量 |
|------|------|
| 总对话轮次 | 360+ |
| 需求变更次数 | 18+ |
| 重大方案调整 | 6 |
| 严重 Bug 修复 | 26+ |
| 环境切换 | 3（家里→公司→多环境） |
| 代码重构 | 4（含环境代码回滚） |
| UI 优化迭代 | 15+ |
| 版本发布 | 5 (v2.1, v2.2, v2.3, v2.3.1, v2.4) |

---

## 沉淀的核心经验

1. **方案分析要区分场景**：全量迁移 vs 子集迁移是不同的问题
2. **流式处理是关键**：大规模数据处理必须避免存储全量
3. **服务端能力要利用**：SCAN MATCH 是服务端过滤，比客户端高效
4. **断点要足够精细**：Slot 级别断点比任务级别可靠得多
5. **错误处理要有上限**：防止错误数据导致 OOM
6. **环境是环境，工具是工具**：不用代码适配特定环境

---

*整合自：CONVERSATION_DRIVEN_EVOLUTION.md、FULL_CONVERSATION_EVOLUTION.md*
*最后更新：v2.4.0 发布后（2026-02-12）*
